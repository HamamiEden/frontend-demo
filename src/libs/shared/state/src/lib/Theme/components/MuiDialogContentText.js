const MuiDialogContentText = mode => {
  return {
    styleOverrides: {
      root: {
        color: "var(--text-secondary)",
        fontSize: 14,
        lineHeight: "21px"
      }
    }
  };
};

export default MuiDialogContentText;
